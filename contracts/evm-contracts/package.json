{
  "name": "@chainlink/contracts",
  "version": "0.0.8",
  "description": "Smart contracts and their language abstractions for chainlink",
  "repository": "https://github.com/smartcontractkit/chainlink",
  "author": "Chainlink devs",
  "license": "MIT",
  "private": false,
  "scripts": {
    "prepare": "yarn clean && yarn build:clean",
    "compile": "belt compile all",
    "build": "yarn compile && tsc -b tsconfig.test.json tsconfig.ethers.json",
    "build:clean": "yarn compile && tsc -b --clean tsconfig.test.json tsconfig.ethers.json",
    "clean": "rm -rf abi ethers truffle",
    "pretest": "tsc -b --clean tsconfig.ethers.json",
    "test": "jest --testTimeout 80000 --forceExit",
    "test:ci": "./scripts/test_ci",
    "prepublishOnly": "yarn clean && yarn build && yarn test:ci"
  },
  "devDependencies": {
    "@chainlink/belt": "0.0.1",
    "@chainlink/test-helpers": "0.0.5",
    "@types/jest": "^26.0.8",
    "@types/node": "^13.9.1",
    "ethers": "^4.0.45",
    "jest": "^25.1.0",
    "ts-jest": "^25.2.0",
    "typescript": "^3.7.5"
  },
  "files": [
    "abi/**/*",
    "ethers/**/*",
    "src/**/*",
    "truffle/**/*",
    "app.config.json"
  ],
  "optionalDependencies": {
    "@truffle/contract": "^4.2.6",
    "ethers": "^4.0.45"
  }
}